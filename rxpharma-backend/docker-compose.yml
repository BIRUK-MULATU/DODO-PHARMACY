services:
  db:
    image: postgres:15
    container_name: rxpharma_db
    environment:
      POSTGRES_DB: rxpharma_db
      POSTGRES_USER: rxpharma_user
      POSTGRES_PASSWORD: rxpharma_pass
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: